#include <string>
using namespace std;
class AddressSet {       // The class
public:             // Access specifier
	string PrivateKey;  // Attribute (string variable)
	string WIF;  // Attribute (string variable)
	string Addresses[3];
};
